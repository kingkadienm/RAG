# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Core Rules

**一次只做一件事。**

这是最高优先级的开发原则。

1. 一个任务只解决一个问题。
2. 一个文件只承担一个职责。
3. 一个类只承担一个职责。
4. 一个方法只完成一个动作。
5. 不允许为了当前任务修改无关代码。
6. 不允许顺手重构其他模块。
7. 不允许为了"架构完整"提前增加抽象。
8. 不允许创建大而全的 Manager、Service、Util、Helper。
9. 发现其他问题，只报告，不处理。
10. 如果一个职责开始变复杂，优先拆分，而不是继续堆代码。
11. 修改代码遵循最小变更原则。
12. 当前任务完成后立即停止。
13. Controller 中不允许编写业务逻辑代码。

## 执行前

先明确：

> 本次任务只解决什么问题？

然后只修改完成这个问题所必须修改的代码。

## 执行中

如果发现：

* 其他 Bug
* 可以重构的代码
* 可以优化的代码
* 其他模块的问题
* 更好的架构方案

不要处理。

记录下来，继续完成当前任务。

## 执行后

只验证当前任务。

不要主动扩展需求。

**禁止过度设计。**

**禁止高耦合。**

**禁止大范围修改。**

**禁止无关重构。**

### 最终原则

> **小任务 → 小修改 → 小职责 → 小依赖。**

> **宁可多几个简单的类，也不要一个什么都负责的大类。**